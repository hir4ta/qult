# 設計: {{taskSlug}}

## アーキテクチャ概要

<!--
ASCII またはコードブロックで全体構成を示す。
コンポーネント間の依存関係、データの流れ、外部システムとの境界を明確にする。
-->

## コンポーネント設計

<!--
各コンポーネントに以下を記載:
- **ファイルパス**: `src/path/to/file.ts`
- **責務**: このコンポーネントが何をするか（1文）
- **インターフェース**: 関数シグネチャ、型定義（TypeScript）
- **依存**: 他コンポーネントとの関係

IDD の原則: Why（なぜこの構成か）と What（外部契約）を記述。
How（内部実装の詳細）は必要最小限に — AI が実装時に判断する。
-->

## データモデル

<!--
新規テーブル、カラム追加、型定義がある場合:
- SQL DDL（テーブル定義、インデックス、制約）
- TypeScript インターフェース
- Migration 戦略（既存データへの影響）
-->

## API 契約

<!--
新規/変更エンドポイントがある場合:
- HTTP メソッド + パス + パラメータ
- リクエスト/レスポンス型
- エラーケース
- Zod スキーマ名（schemas.ts に追加する場合）
-->

## 設計判断

<!--
| 判断 | 選択 | 理由 | 代替案 |
|------|------|------|--------|

重要な判断は `ledger save sub_type=decision` で永続化する。
設計判断は compact を超えて生存する唯一のコンテキスト。
-->

## 制約 & 非目標

<!--
この設計が意図的に対応しないもの:
- 「作らないもの」を明示することで AI のスコープ逸脱を防止
- requirements.md の Out of Scope と整合させる
-->

## トレーサビリティマトリクス

<!--
全 FR が設計コンポーネント → タスク → テストに紐づくことを確認する。
孤立した FR（テストなし）や孤立したタスク（FR なし）がないこと。
-->

| Req ID | Component | Task ID | Test ID |
|--------|-----------|---------|---------|
| FR-1   |           | T-1.1   | TS-1.1  |
