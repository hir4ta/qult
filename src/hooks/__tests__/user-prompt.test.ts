// checkSpecRequired was removed in v0.7.7 (moved to rules).
// UserPromptSubmit command hook now only handles knowledge search.
// No unit-testable exported functions remain.
import { describe, it } from "vitest";

describe("userPromptSubmit", () => {
	it.todo("knowledge search integration test");
});
