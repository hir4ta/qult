/**
 * TUI entry point (v2 stub).
 * TODO (Phase 4): Implement quality dashboard TUI.
 */

export async function runTui(): Promise<void> {
	console.log("alfred v2 TUI — not yet implemented (Phase 4)");
	console.log("Use `alfred doctor` to check installation health.");
}
