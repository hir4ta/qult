{
  "name": "qult",
  "version": "0.17.0",
  "type": "module",
  "files": [
    "dist/"
  ],
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "build": "bun build.ts",
    "dev": "bun --watch src/hook-entry.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "lint:fix": "biome check --write src/",
    "test": "bunx --bun vitest run",
    "test:watch": "bunx --bun vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "@types/bun": "^1.3.11",
    "typescript": "^5.8",
    "vitest": "^3"
  }
}
