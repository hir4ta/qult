# Build output
dist/
dist/binaries/
node_modules/
web/dist/
web/node_modules/
.task/

# Lock files (bun.lock is tracked; npm lock files are not)
package-lock.json
web/package-lock.json

# Database
*.db

# OS
.DS_Store

# Editor
*.swp
*.swo
*~

# Config (local)
.claude/settings.json
# .alfred/ specs and knowledge are git-tracked (root only)
# State files and non-root .alfred/ are excluded
.alfred/.state/
.alfred/.pending-compact.json
web/.alfred/

# Test
coverage/
*.tsbuildinfo

# plugin/ is git-tracked for marketplace distribution.
# Source of truth: content/ (hooks, mcp config).

# serena mcp 
.serena
